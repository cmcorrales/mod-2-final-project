<%= render partial:"navigator" %>
<br/>
<h1>There are <%= @questions.count %> pending questions, or you can <%= link_to "Create New Question", new_course_question_path(@course.id) %></h1>
<% @questions.each do |question| %>
  <h3><%= link_to question.title, course_question_path(question.course_id, question.id)%></h3>
<% end %>
